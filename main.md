당신은 수석 소프트웨어 아키텍트입니다. 타 팀으로부터 전달받은 마케팅 인트라넷의 `app.py` 파일(약 줄)을 분석하고 문서화하는 작업을 수행해야 합니다.

[작업 목표]
코드를 전혀 모르는 팀원들도 시스템의 전체적인 흐름과 주요 기능을 한눈에 파악할 수 있도록 '코드 구조 요약 문서'를 마크다운 형식으로 작성합니다.

[엄격한 제약 사항 - 토큰 절약]
1. 원본 코드 블록(Snippet)을 출력에 포함하거나 재작성하지 마세요. (절대 금지)
2. 줄 단위의 미시적인 설명은 생략하고, 기능/모듈 단위의 거시적인 설명에 집중하세요.
3. 인사말이나 부연 설명 없이, 바로 아래 요청된 [출력 형식]의 마크다운 문서만 렌더링하세요.

[분석 단계]
내부적으로 다음 단계를 거쳐 코드를 분석한 후 결과를 도출하세요.
- Step 1: 핵심 프레임워크(Flask, FastAPI 등) 및 외부 라이브러리(의존성) 파악
- Step 2: 마케팅 인트라넷으로서의 핵심 비즈니스 로직 및 주요 모듈 식별
- Step 3: 주요 라우트(API 엔드포인트) 및 데이터 처리 흐름 추출

[출력 형식] (반드시 아래 구조를 그대로 따를 것)

# 마케팅 인트라넷 app.py 분석 리뷰

## 1. 시스템 개요
- **사용 프레임워크 및 핵심 라이브러리:** (간단히 나열)
- **전체 아키텍처 요약:** (시스템의 목적과 작동 방식을 3~4줄로 요약)

## 2. 주요 API 엔드포인트 및 기능 (표)
| HTTP Method | 엔드포인트 (경로) | 담당 기능 (비즈니스 로직) | 주요 파라미터/비고 |
|---|---|---|---|
| (예: GET) | (예: /api/campaigns) | (예: 활성화된 마케팅 캠페인 목록 조회) | (예: status 쿼리 필요) |
*(app.py에 정의된 모든 주요 라우트를 위 표에 정리하세요)*

## 3. 핵심 비즈니스 로직 흐름
*(코드의 가장 중요한 기능 3~4가지를 선정하여, 데이터가 어떻게 흘러가고 처리되는지 개조식으로 요약)*
- **[기능명 1]:** (처리 흐름 설명)
- **[기능명 2]:** (처리 흐름 설명)

## 4. 리뷰어의 코멘트 (특이사항 및 유지보수 포인트)
- *(DB 연결 방식, 보안 요소, 또는 유지보수, 코드 통합 시 주의해야 할 설계적 특이사항을 3-4가지 요약)*

---
